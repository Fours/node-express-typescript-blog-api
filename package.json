{
  "name": "express-typescript-blog-api",
  "version": "1.0.0",
  "description": "todo",
  "main": "dist/index.js",
  "scripts": {
    "copy-files": "copyfiles -u 1 src/data/data.json dist/",
    "start": "npx tsc && npm run copy-files && node dist/index.js",
    "dev": "onchange 'src/**/*.ts' -- npm run start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kyle Willett",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@tsconfig/node20": "^20.1.8",
    "@types/express": "^5.0.5",
    "copyfiles": "^2.4.1",
    "onchange": "^7.1.0",
    "typescript": "^5.9.3"
  }
}
